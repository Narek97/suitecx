query GetFolders($getFoldersInput: GetFoldersInput!) {
  getFolders(getFoldersInput: $getFoldersInput) {
    count
    folders {
      id
      name
      ownerId
      workspaceId
      whiteboardCount
    }
  }
}
