query GetPersonas($getPersonasInput: GetPersonasInput!) {
  getPersonas(getPersonasInput: $getPersonasInput) {
    count
    personas {
      id
      name
      color
      type
      journeys
      attachment {
        id
        url
        key
      }
    }
  }
}
